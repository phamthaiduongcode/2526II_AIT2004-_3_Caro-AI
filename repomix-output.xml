This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gemini/skill.md
.gitignore
assets/.gitkeep
details.md
gui/analysis.py
gui/button.py
gui/interface.py
play.py
README.md
requirements.txt
source/AI.py
source/benchmark.py
source/gomoku.py
source/train.py
source/utils.py
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".gemini/skill.md">
🤖 System Prompt: Caro AI Coding Assistant[Vai trò & Mục tiêu]Đóng vai là một Senior Software Engineer và AI Mentor. Nhiệm vụ của bạn là hỗ trợ nhóm sinh viên đại học xây dựng một chương trình Cờ Caro tích hợp AI (thuật toán Minimax và Alpha-Beta Pruning). Mục tiêu cốt lõi là giúp nhóm tạo ra một codebase chất lượng cao, tuân thủ nghiêm ngặt các nguyên lý Clean Code, Design Patterns và đạt kết quả học tập xuất sắc. Bạn đóng vai trò hướng dẫn, không phải là người code hộ toàn bộ dự án.[Quy tắc cốt lõi khi hỗ trợ Code]1. Đảm bảo tính minh bạch và Hiểu biết (Academic Integrity)Không cung cấp toàn bộ file mã nguồn hoàn chỉnh hoặc sao chép nguyên trạng từ các repo mẫu.Khi đề xuất một đoạn code (đặc biệt là thuật toán đệ quy Minimax, Alpha-Beta hoặc hàm Heuristic), bắt buộc phải giải thích chi tiết cách hoạt động, độ phức tạp thời gian và lý do chọn phương pháp đó để sinh viên có thể tự tin bảo vệ thuật toán của mình.  2. Tuân thủ nghiêm ngặt Luật chơi Caro cốt lõiĐiều kiện thắng: Chính xác 4 quân liên tiếp theo hàng ngang, hàng dọc, hoặc đường chéo.  Tuyệt đối không xét luật chặn hai đầu trong mọi logic kiểm tra thắng thua hoặc đánh giá điểm số.  Bàn cờ phải hỗ trợ kích thước linh hoạt, tối thiểu là 9x9.  3. Kiến trúc hệ thống & Clean CodeHướng dẫn áp dụng các Design Patterns phù hợp và bám sát thiết kế UML ban đầu.Bắt buộc phân tách rõ ràng các module: UI (Giao diện người dùng), Game Logic (Luật chơi, trạng thái bàn cờ) và AI Agent (Thuật toán tìm kiếm). Code giao diện không được trộn lẫn với logic đệ quy.  Sử dụng ngôn ngữ Java (kết hợp JavaFX) hoặc Python (kết hợp Pygame) một cách tối ưu, chú trọng việc quản lý bộ nhớ và tối ưu vòng lặp game.4. Ràng buộc về thuật toán AIKhi cài đặt Alpha-Beta pruning, phải đảm bảo thuật toán này cắt nhánh ngay khi giá trị $\beta \le \alpha$.  Để phục vụ việc benchmark, thuật toán Minimax và Alpha-Beta bắt buộc phải dùng chung một hàm đánh giá (heuristic) và cùng một độ sâu tìm kiếm (depth limit) khi được gọi so sánh.  Mọi hàm AI phải trả về không chỉ giá trị đánh giá (score) mà còn phải đi kèm nước đi tốt nhất tương ứng.  5. Hỗ trợ Đo đạc và Thực nghiệm (Data Engineering)Hỗ trợ thiết kế các hàm đếm số trạng thái đã xét và đo thời gian chạy thực thi của mỗi nước đi.  Gợi ý cách xuất dữ liệu log ra định dạng CSV để nhóm có thể dễ dàng sử dụng Python (Pandas) kết hợp Jupyter Notebook phân tích, xử lý dữ liệu và vẽ biểu đồ cho báo cáo thực nghiệm.[Cách thức tương tác]Khi người dùng hỏi cách giải quyết một bug (ví dụ: vòng lặp game bị đứng, đệ quy vô hạn), hãy chỉ ra nguyên nhân gốc rễ và đưa ra gợi ý sửa lỗi thay vì chỉ ném ra đoạn code đã sửa.Luôn nhắc nhở nhóm kiểm tra các trường hợp biên (edge cases) như: bàn cờ đầy không có ai thắng (hòa), chọn nước đi khi mảng trạng thái trống, v.v.
</file>

<file path=".gitignore">
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#   Usually these files are written by a python script from a template
#   before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
# Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
# uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
# poetry.lock
# poetry.toml

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   pdm recommends including project-wide configuration in pdm.toml, but excluding .pdm-python.
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
# pdm.lock
# pdm.toml
.pdm-python
.pdm-build/

# pixi
#   Similar to Pipfile.lock, it is generally recommended to include pixi.lock in version control.
# pixi.lock
#   Pixi creates a virtual environment in the .pixi directory, just like venv module creates one
#   in the .venv directory. It is recommended not to include this directory in version control.
.pixi

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# Redis
*.rdb
*.aof
*.pid

# RabbitMQ
mnesia/
rabbitmq/
rabbitmq-data/

# ActiveMQ
activemq-data/

# SageMath parsed files
*.sage.py

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#   JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#   be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#   and can be added to the global gitignore or merged into this file.  For a more nuclear
#   option (not recommended) you can uncomment the following to ignore the entire idea folder.
# .idea/

# Abstra
#   Abstra is an AI-powered process automation framework.
#   Ignore directories containing user credentials, local state, and settings.
#   Learn more at https://abstra.io/docs
.abstra/

# Visual Studio Code
#   Visual Studio Code specific template is maintained in a separate VisualStudioCode.gitignore 
#   that can be found at https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
#   and can be added to the global gitignore or merged into this file. However, if you prefer, 
#   you could uncomment the following to ignore the entire vscode folder
# .vscode/
# Temporary file for partial code execution
tempCodeRunnerFile.py

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# Streamlit
.streamlit/secrets.toml
</file>

<file path="assets/.gitkeep">

</file>

<file path="details.md">
# Chi tiết kỹ thuật Dự án Caro AI

Tài liệu này cung cấp cái nhìn sâu hơn về cấu trúc, thuật toán và các lưu ý quan trọng dành cho nhà phát triển.

## 1. Chi tiết các Module

### `source/gomoku.py` (Logic bàn cờ)
- **Lớp `Board`**: Quản lý trạng thái bàn cờ bằng mảng 2 chiều.
- **Luật chơi**: Thắng khi có **đúng 4 quân liên tiếp** theo bất kỳ hướng nào (ngang, dọc, chéo). 
- **Không xét luật chặn hai đầu**: Đây là yêu cầu đặc thù của đề bài.
- **`get_legal_moves`**: Tối ưu hóa bằng cách chỉ xét các ô trống trong bán kính 2 ô xung quanh các quân đã đánh để giảm không gian trạng thái.

### `source/AI.py` (Bộ não AI)
- **`minimax`**: Thuật toán tìm kiếm đệ quy cơ bản.
- **`alpha_beta`**: Phiên bản cải tiến giúp cắt nhánh các vùng không gian không cần thiết khi $\beta \le \alpha$.
- **Hàm đánh giá (Heuristic)**: 
    - Sử dụng kỹ thuật "cửa sổ trượt" (Sliding Window) kích thước 4.
    - Gán điểm theo trọng số mũ (100, 1000, 100000) dựa trên số lượng quân cờ trong cửa sổ.
    - Ưu tiên phòng thủ bằng cách nhân hệ số cho điểm số của đối thủ.
- **Move Ordering**: Sắp xếp các nước đi ưu tiên từ tâm bàn cờ ra ngoài để tối ưu hóa việc cắt nhánh sớm cho Alpha-Beta.

### `source/utils.py` (Tiện ích)
- **`GameLogger`**: Ghi lại lịch sử thực nghiệm (Algorithm, Depth, Nodes, Time, Score) vào file CSV trong thư mục `logs/`. Dữ liệu này được dùng để vẽ biểu đồ so sánh.

### `source/benchmark.py` (Thực nghiệm)
- Chứa 5 kịch bản (Test Cases) từ bàn cờ trống đến các thế cờ hiểm.
- Dùng để chứng minh tính đúng đắn: Alpha-Beta phải trả về cùng kết quả với Minimax nhưng duyệt ít trạng thái hơn.

## 2. Luồng hoạt động của chương trình (Flow)

1. **Khởi tạo**: `play.py` khởi tạo đối tượng `Board` và `CaroAI`.
2. **Vòng lặp chính**:
    - Nếu là lượt Người: Nhận input -> `Board.make_move()` -> Kiểm tra thắng/thua.
    - Nếu là lượt AI:
        - `AI.get_move()` được gọi.
        - Thuật toán (Minimax/Alpha-Beta) duyệt cây trạng thái đến độ sâu quy định.
        - Mỗi bước thử nghiệm dùng `Board.make_move()` và `Board.undo_move()`.
        - Trả về nước đi tốt nhất.
    - **Ghi Log**: Thông tin nước đi được `GameLogger` lưu lại.
3. **Kết thúc**: Hiển thị kết quả thắng/thua hoặc hòa.

## 3. Các vấn đề Dev cần nắm vững (FAQ)

### Tại sao AI chạy chậm?
- Kiểm tra `depth` (độ sâu). Với Python, độ sâu 3-4 là giới hạn cho bàn cờ lớn nếu không tối ưu tốt.
- Kiểm tra hàm `get_legal_moves`. Nếu không giới hạn vùng tìm kiếm, số node sẽ tăng theo hàm mũ $O(b^d)$.

### Tại sao Alpha-Beta không nhanh hơn Minimax?
- Alpha-Beta chỉ hiệu quả nhất khi tìm thấy nước đi tốt nhất sớm. Nếu không có **Move Ordering** (sắp xếp nước đi), số node có thể vẫn tương đương Minimax.

### Lưu ý về luật "4 quân liên tiếp"
- Logic `check_win` hiện tại sẽ dừng ngay khi đếm đủ 4. Nếu trong thực tế người chơi đánh thành 5 quân, thuật toán vẫn ghi nhận là thắng tại quân thứ 4. Đây là điểm cần lưu ý khi giải trình với giảng viên về subset của chuỗi thắng.

### Cách đọc Log thực nghiệm
- Mở `logs/benchmark_results.csv` bằng Excel hoặc dùng thư viện Pandas để vẽ biểu đồ.
- Trọng tâm phân tích: Cột `Nodes_Visited` của Alpha-Beta phải nhỏ hơn đáng kể so với Minimax.

## 4. Các trường hợp biên (Edge Cases)
- **Bàn cờ đầy**: Hàm `check_win` trả về -1 (Hòa).
- **Nước đi đầu tiên**: AI được lập trình đánh vào giữa bàn cờ nếu là người đi đầu.
- **Chặn đối thủ**: Heuristic được tinh chỉnh để ưu tiên chặn chuỗi 3 của đối thủ hơn là tạo chuỗi 2 cho mình.
</file>

<file path="gui/analysis.py">
import pandas as pd
import matplotlib.pyplot as plt
import os

def plot_results(csv_path="logs/benchmark_results.csv"):
    if not os.path.exists(csv_path):
        print("Chưa có dữ liệu benchmark. Hãy chạy tests/benchmark.py trước!")
        return

    df = pd.read_csv(csv_path)
    os.makedirs('experiments', exist_ok=True)
    
    # Tách dữ liệu theo thuật toán
    # Lưu ý: CSV của chúng ta ghi Algorithm theo dạng "Minimax_StateName"
    df['Algo_Type'] = df['Algorithm'].apply(lambda x: 'Minimax' if 'Minimax' in x else 'AlphaBeta')
    df['State'] = df['Algorithm'].apply(lambda x: x.split('_', 1)[1] if '_' in x else x)

    # Vẽ biểu đồ so sánh số Nodes Visited (lấy trung bình nếu có nhiều lần chạy)
    pivot_df = df.groupby(['State', 'Algo_Type'])['Nodes_Visited'].mean().unstack()
    pivot_df.plot(kind='bar', figsize=(10, 6))
    
    plt.title('So sánh số trạng thái đã duyệt (Nodes Visited)')
    plt.ylabel('Số lượng Nodes')
    plt.xlabel('Trạng thái bàn cờ')
    plt.xticks(rotation=45)
    plt.grid(axis='y', linestyle='--', alpha=0.7)
    plt.savefig('experiments/nodes_comparison.png')
    print("Đã lưu biểu đồ tại experiments/nodes_comparison.png")

if __name__ == "__main__":
    plot_results()
</file>

<file path="gui/button.py">
import pygame

class Button:
    def __init__(self, x, y, width, height, text, color=(200, 200, 200)):
        self.rect = pygame.Rect(x, y, width, height)
        self.text = text
        self.color = color
        self.font = pygame.font.SysFont('Arial', 24)

    def draw(self, screen):
        pygame.draw.rect(screen, self.color, self.rect)
        pygame.draw.rect(screen, (0, 0, 0), self.rect, 2)
        text_surf = self.font.render(self.text, True, (0, 0, 0))
        text_rect = text_surf.get_rect(center=self.rect.center)
        screen.blit(text_surf, text_rect)

    def is_clicked(self, pos):
        return self.rect.collidepoint(pos)
</file>

<file path="requirements.txt">
pygame>=2.0.0
pandas>=1.3.0
matplotlib>=3.4.0
</file>

<file path="source/train.py">
import sys
import os
import random
import copy

# Thêm thư mục gốc vào path để import được các module từ gói source
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from source.gomoku import Board
from source.AI import CaroAI
from source.utils import TrainingLogger

def play_match(ai1_weights, ai1_def, ai2_weights, ai2_def, rounds=5):
    """
    Tăng số ván đấu và sử dụng 2-3 nước đi đầu ngẫu nhiên để tạo sự bất đối xứng.
    """
    score1, score2 = 0, 0
    size = 9
    for _ in range(rounds):
        # Tạo thế trận khởi đầu 3 nước ngẫu nhiên để phá thế hòa
        start_moves = []
        temp_board = Board(size)
        for _ in range(3):
            r, c = random.randint(2, size-3), random.randint(2, size-3)
            if temp_board.make_move(r, c):
                start_moves.append((r, c))
        
        for swap in [False, True]:
            board = Board(size)
            for r, c in start_moves: board.make_move(r, c)

            p1_is_challenger = not swap
            # Xác định AI nào cầm quân nào dựa trên lượt hiện tại của bàn cờ
            cid = board.current_player if p1_is_challenger else 3 - board.current_player
            bid = 3 - cid
            
            # Dùng depth 3 để chạy nhanh hơn, cho phép chạy nhiều ván hơn
            agent_c = CaroAI(player_id=cid, depth=3, weights=ai1_weights, defense_multiplier=ai1_def)
            agent_b = CaroAI(player_id=bid, depth=3, weights=ai2_weights, defense_multiplier=ai2_def)

            while board.check_win() == 0:
                curr = agent_c if board.current_player == cid else agent_b
                move, _, _, _ = curr.get_move(board, mode="alpha_beta", time_limit=0.2)
                if move:
                    board.make_move(move[0], move[1])
                else: break
            
            winner = board.check_win()
            if winner == cid:
                score1 += 2
            elif winner == bid:
                score2 += 2
            elif winner == -1:
                score1 += 1
                score2 += 1
            
    return score1 > score2

def evolve():
    # Khởi đầu với trọng số nhạy bén hơn (có cả quân đơn lẻ)
    best_weights = {3: 11396, 2: 529, 1: 76}
    best_def = 1.51
    # Khởi tạo logger huấn luyện
    train_logger = TrainingLogger()
    
    print(f"Bắt đầu huấn luyện... Gốc: {best_weights}, Def: {best_def}")

    for generation in range(1, 1000): # Tăng số thế hệ lên để AI có thời gian tiến hóa
        # Tạo biến dị (Mutation)
        challenger_weights = {
            3: max(5000, best_weights[3] + random.randint(-800, 800)), # Biến dị nhỏ hơn để tinh chỉnh
            2: max(200, best_weights[2] + random.randint(-100, 100)),
            1: max(10, best_weights.get(1, 50) + random.randint(-10, 10))
        }
        challenger_def = round(max(1.2, best_def + random.uniform(-0.2, 0.2)), 2)
        
        print(f"\nThế hệ {generation}: Challenger thử nghiệm {challenger_weights}, Def: {challenger_def}")
        
        # Chơi 3 vòng (6 ván đấu) để đánh giá
        is_better = play_match(challenger_weights, challenger_def, best_weights, best_def, rounds=3)
        
        # Ghi log kết quả thế hệ này vào file CSV
        train_logger.log_generation(generation, challenger_weights, challenger_def, is_better, best_weights, best_def)

        if is_better:
            print(f"--> [MỚI] Challenger chiến thắng! Cập nhật bộ trọng số.")
            best_weights = challenger_weights
            best_def = challenger_def
        else:
            print(f"--> Giữ lại bộ trọng số cũ.")

    print("\n" + "="*30)
    print(f"KẾT QUẢ HUẤN LUYỆN CUỐI CÙNG:")
    print(f"Weights: {best_weights}")
    print(f"Defense Multiplier: {best_def}")
    print("="*30)
    print("Bạn có thể copy bộ số này vào file AI.py để máy đánh khôn hơn!")

if __name__ == "__main__":
    evolve()
</file>

<file path="gui/interface.py">
import pygame
import sys
import threading
import copy
from source.gomoku import Board
from source.AI import CaroAI

# Cấu hình màu sắc và kích thước
BOARD_SIZE = 9
CELL_SIZE = 60
MARGIN = 40
WINDOW_SIZE = BOARD_SIZE * CELL_SIZE + 2 * MARGIN
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
GRAY = (200, 200, 200)
RED = (200, 0, 0)
BLUE = (0, 0, 200)

class CaroGUI:
    def __init__(self, board, ai):
        pygame.init()
        self.screen = pygame.display.set_mode((WINDOW_SIZE, WINDOW_SIZE + 50))
        pygame.display.set_caption("UET Caro AI - 4 in a row")
        self.font = pygame.font.SysFont('Arial', 20)
        self.board = board
        self.ai = ai
        self.game_over = False
        self.ai_thinking = False

    def draw_board(self):
        self.screen.fill(WHITE)
        # Vẽ lưới
        for i in range(BOARD_SIZE + 1):
            start_pos_v = (MARGIN + i * CELL_SIZE, MARGIN)
            end_pos_v = (MARGIN + i * CELL_SIZE, MARGIN + BOARD_SIZE * CELL_SIZE)
            pygame.draw.line(self.screen, BLACK, start_pos_v, end_pos_v, 1)

            start_pos_h = (MARGIN, MARGIN + i * CELL_SIZE)
            end_pos_h = (MARGIN + BOARD_SIZE * CELL_SIZE, MARGIN + i * CELL_SIZE)
            pygame.draw.line(self.screen, BLACK, start_pos_h, end_pos_h, 1)

        # Vẽ quân cờ
        for r in range(BOARD_SIZE):
            for c in range(BOARD_SIZE):
                center = (MARGIN + c * CELL_SIZE + CELL_SIZE // 2, MARGIN + r * CELL_SIZE + CELL_SIZE // 2)
                if self.board.grid[r][c] == 1: # Người (X)
                    pygame.draw.line(self.screen, RED, (center[0]-20, center[1]-20), (center[0]+20, center[1]+20), 3)
                    pygame.draw.line(self.screen, RED, (center[0]+20, center[1]-20), (center[0]-20, center[1]+20), 3)
                elif self.board.grid[r][c] == 2: # Máy (O)
                    pygame.draw.circle(self.screen, BLUE, center, 22, 3)

    def ai_move_wrapper(self):
        """Hàm bọc để chạy AI trong Thread riêng."""
        # QUAN TRỌNG: AI phải tính toán trên bản sao để không làm loạn bàn cờ thật
        board_copy = copy.deepcopy(self.board)
        move, _, _, _ = self.ai.get_move(board_copy, mode="alpha_beta")
        
        if move and not self.game_over:
            # Thực hiện nước đi thật trên bàn cờ chính
            pygame.event.post(pygame.event.Event(pygame.USEREVENT, {"move": move}))
        self.ai_thinking = False

    def run(self):
        while True:
            self.draw_board()
            
            for event in pygame.event.get():
                if event.type == pygame.QUIT:
                    pygame.quit()
                    sys.exit()

                if not self.game_over and self.board.current_player == 1 and event.type == pygame.MOUSEBUTTONDOWN:
                    x, y = event.pos
                    c = (x - MARGIN) // CELL_SIZE
                    r = (y - MARGIN) // CELL_SIZE
                    if self.board.make_move(r, c):
                        winner = self.board.check_win()
                        if winner != 0: self.game_over = True

                if event.type == pygame.USEREVENT:
                    if event.move is None:
                        # Nếu AI không có nước đi, kiểm tra thắng thua để kết thúc game
                        if self.board.check_win() != 0:
                            self.game_over = True
                    else:
                        r, c = event.move
                        if self.board.make_move(r, c):
                            if self.board.check_win() != 0: self.game_over = True

            # Lượt của AI
            if not self.game_over and self.board.current_player == 2 and not self.ai_thinking:
                self.ai_thinking = True
                threading.Thread(target=self.ai_move_wrapper, daemon=True).start()

            if self.game_over:
                msg = "Game Over!"
                winner = self.board.check_win()
                if winner == 1: msg = "Ban thang!"
                elif winner == 2: msg = "AI thang!"
                elif winner == -1: msg = "Hoa!"
                text = self.font.render(msg, True, BLACK)
                self.screen.blit(text, (WINDOW_SIZE // 2 - 50, WINDOW_SIZE + 10))

            pygame.display.flip()

if __name__ == "__main__":
    # Test nhanh UI
    from source.gomoku import Board
    from source.AI import CaroAI
    b = Board(9)
    a = CaroAI(player_id=2, depth=2)
    gui = CaroGUI(b, a)
    gui.run()
</file>

<file path="README.md">
# 2526II_AIT2004-_3_Caro-AI

Chương trình chơi cờ Caro (4 in a row) tích hợp trí tuệ nhân tạo sử dụng thuật toán Minimax và Alpha-Beta Pruning.

## 📌 Tính năng chính
- Chế độ chơi: Người đấu với Máy (Console).
- Thuật toán AI: Minimax và Alpha-Beta Pruning.
- Tối ưu hóa: Move Ordering (ưu tiên trung tâm) và giới hạn vùng tìm kiếm.
- Hệ thống Log: Tự động ghi lại thời gian chạy và số trạng thái đã xét ra file CSV.
- Benchmark: Công cụ đo đạc hiệu năng tự động trên các kịch bản mẫu.

## 🛠 Cài đặt
Yêu cầu: Python 3.8 trở lên.

```bash
# Clone project
git clone <link-repo>
cd 2526II_AIT2004-_3_Caro-AI
```

## 🚀 Hướng dẫn sử dụng

### 1. Chạy trò chơi (Chế độ Console & GUI)
Đây là file khởi đầu chính của dự án:
```bash
python play.py
```
*Lưu ý: Bạn nhập nước đi theo định dạng `hàng cột` (ví dụ: `4 4`).*

### 2. Chạy Benchmark thực nghiệm
Để so sánh hiệu năng giữa Minimax và Alpha-Beta:
```bash
python source/benchmark.py
```
Kết quả sẽ được hiển thị ngay trên màn hình và lưu vào `logs/benchmark_results.csv`.

## 📂 Cấu trúc thư mục
Vui lòng xem chi tiết tại file details.md để hiểu rõ chức năng của từng module và luồng hoạt động của chương trình.

---
*Dự án được thực hiện trong khuôn khổ môn học Cơ sở Trí tuệ nhân tạo - UET.*
</file>

<file path="source/benchmark.py">
import sys
import os
import copy

# Thêm thư mục gốc vào path để import được các module từ source
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from source.gomoku import Board
from source.AI import CaroAI
from source.utils import GameLogger

def run_benchmark():
    # Sử dụng file log riêng cho benchmark để không lẫn với log khi chơi
    logger = GameLogger(filename="logs/benchmark_results.csv")
    size = 9
    ai_depth = 3 # Độ sâu 3 là đủ để thấy sự khác biệt rõ rệt về số node

    # Danh sách các kịch bản thử nghiệm
    test_cases = []

    # 1. Trạng thái trống (Bắt đầu ván - kiểm tra tốc độ khởi đầu)
    s1 = Board(size)
    s1.current_player = 2 # Giả định lượt của AI (O)
    test_cases.append(("Empty_Board", s1))

    # 2. Trạng thái AI có thể thắng ngay (Cần đánh vào hàng 0 để đủ 4 quân 'O')
    s2 = Board(size)
    for c in range(3): 
        s2.grid[0][c] = 2 # AI (O) đã có 3 quân
        s2.history.append((0, c, 2))
    s2.current_player = 2
    test_cases.append(("AI_Win_Move", s2))

    # 3. Trạng thái AI phải chặn người chơi (Người chơi đã có 3 quân 'X')
    s3 = Board(size)
    for c in range(3): 
        s3.grid[4][c+3] = 1 # Người chơi (X) đã có 3 quân
        s3.history.append((4, c+3, 1))
    s3.current_player = 2
    test_cases.append(("AI_Must_Block", s3))

    # 4. Trạng thái giữa ván (Giao tranh phức tạp ở trung tâm)
    s4 = Board(size)
    moves = [(4,4,1), (4,5,2), (5,5,1), (3,3,2), (5,4,1), (3,4,2)]
    for r, c, p in moves:
        s4.grid[r][c] = p
        s4.history.append((r, c, p))
    s4.current_player = 2
    test_cases.append(("Mid_Game_Battle", s4))

    # 5. Trạng thái nhiều lựa chọn (Kiểm tra khả năng lọc nước đi)
    s5 = Board(size)
    s5.grid[0][0] = 1; s5.grid[8][8] = 2
    s5.history = [(0,0,1), (8,8,2)]
    s5.current_player = 2
    test_cases.append(("Sparse_Board", s5))

    print(f"{'Algorithm':<15} | {'State':<15} | {'Nodes':<10} | {'Time (s)':<10} | {'Move'}")
    print("-" * 65)

    for name, board_state in test_cases:
        # Chạy Minimax
        ai_minimax = CaroAI(player_id=2, depth=ai_depth)
        board_m = copy.deepcopy(board_state) # Copy để không làm hỏng state gốc
        move_m, score_m, nodes_m, time_m = ai_minimax.get_move(board_m, mode="minimax")
        logger.log_result(f"Minimax_{name}", ai_depth, nodes_m, time_m, score_m, move_m)
        print(f"{'Minimax':<15} | {name[:15]:<15} | {nodes_m:<10} | {time_m:.4f} | {move_m}")

        # Chạy Alpha-Beta
        ai_ab = CaroAI(player_id=2, depth=ai_depth)
        board_ab = copy.deepcopy(board_state)
        move_ab, score_ab, nodes_ab, time_ab = ai_ab.get_move(board_ab, mode="alpha_beta")
        logger.log_result(f"AlphaBeta_{name}", ai_depth, nodes_ab, time_ab, score_ab, move_ab)
        print(f"{'Alpha-Beta':<15} | {name[:15]:<15} | {nodes_ab:<10} | {time_ab:.4f} | {move_ab}")
        
        # Kiểm tra tính đúng đắn: Minimax và Alpha-Beta PHẢI chọn cùng nước đi và điểm số
        if move_m != move_ab:
            print(f"--> [!] Cảnh báo: Kết quả nước đi khác nhau tại {name}!")

    print("\nBenchmark hoàn tất! Kết quả đã được lưu vào logs/benchmark_results.csv")

if __name__ == "__main__":
    run_benchmark()
</file>

<file path="source/gomoku.py">
import random

class Board:
    def __init__(self, size=15):
        self.size = size
        # Khởi tạo bàn cờ với toàn bộ giá trị là 0 (ô trống)
        self.grid = [[0 for _ in range(size)] for _ in range(size)]
        self.current_player = 1  # 1 là X, 2 là O
        self.history = []  # Lưu lịch sử để phục vụ AI (Undo move)

        # Khởi tạo Zobrist Hashing để nhận diện trạng thái bàn cờ
        # Mỗi ô có 3 trạng thái: trống (0), X (1), O (2)
        self.zobrist_table = [[[random.getrandbits(64) for _ in range(3)] 
                               for _ in range(size)] for _ in range(size)]
        self.current_hash = random.getrandbits(64)
        # Giá trị XOR để phân biệt lượt đi (Side to move)
        self.zobrist_side = random.getrandbits(64)
        # Cờ đánh dấu AI đang tính toán, tránh log tràn lan terminal
        self.is_searching = False

    def get_hash(self):
        return self.current_hash

    def is_valid_move(self, row, col):
        """Kiểm tra nước đi có nằm trong bàn cờ và ô đó có trống không."""
        return 0 <= row < self.size and 0 <= col < self.size and self.grid[row][col] == 0

    def check_win(self):
        """Kiểm tra xem có ai thắng chưa (4 quân liên tiếp)."""
        # Tối ưu: Chỉ kiểm tra xung quanh nước đi cuối cùng trong history
        if not self.history:
            return 0
        r, c, p = self.history[-1]
        win_player = self._check_at(r, c)
        if win_player != 0:
            return win_player
            
        if len(self.history) == self.size * self.size:
            return -1 # Hòa
        return 0 # Chưa kết thúc

    def _check_at(self, r, c):
        """Kiểm tra thắng thua tại một vị trí cụ thể (Tối ưu O(1))."""
        player = self.grid[r][c]
        if player == 0: return 0
        
        for dr, dc in [(0, 1), (1, 0), (1, 1), (1, -1)]:
            count = 1
            # Kiểm tra hai phía của quân cờ vừa đánh
            for i in range(1, 4):
                nr, nc = r + dr * i, c + dc * i
                if 0 <= nr < self.size and 0 <= nc < self.size and self.grid[nr][nc] == player:
                    count += 1
                else: break
            for i in range(1, 4):
                nr, nc = r - dr * i, c - dc * i
                if 0 <= nr < self.size and 0 <= nc < self.size and self.grid[nr][nc] == player:
                    count += 1
                else: break
            
            if count >= 4: return player
        return 0

    def make_move(self, row, col):
        """Thực hiện nước đi và chuyển lượt."""
        if self.is_valid_move(row, col):
            # Update hash: XOR quân cờ cũ (0) và quân cờ mới
            self.current_hash ^= self.zobrist_table[row][col][self.grid[row][col]]
            self.grid[row][col] = self.current_player
            self.current_hash ^= self.zobrist_table[row][col][self.current_player]
            
            self.current_hash ^= self.zobrist_side # XOR lượt đi
            
            self.history.append((row, col, self.current_player))
            
            # Chuyển lượt: nếu là 1 thì thành 2, nếu là 2 thì thành 1
            self.current_player = 3 - self.current_player
            return True
        return False

    def undo_move(self):
        """Hoàn tác nước đi cuối cùng (Rất quan trọng cho thuật toán AI)."""
        if not self.history:
            return
        
        row, col, player = self.history.pop()
        # Update hash ngược lại
        self.current_hash ^= self.zobrist_table[row][col][self.grid[row][col]]
        self.grid[row][col] = 0
        self.current_hash ^= self.zobrist_table[row][col][0]
        
        self.current_hash ^= self.zobrist_side # XOR lượt đi
        
        self.current_player = player # Quay lại lượt của người vừa đánh

    def display(self):
        """Hiển thị bàn cờ cơ bản ra console để debug."""
        print("  " + " ".join([f"{i:x}" for i in range(self.size)])) # In chỉ số cột (hex)
        for r in range(self.size):
            row_str = f"{r:x} "
            for c in range(self.size):
                val = self.grid[r][c]
                if val == 0: row_str += ". "
                elif val == 1: row_str += "X "
                else: row_str += "O "
            print(row_str)

    def get_legal_moves(self):
        """Lấy danh sách các nước đi khả thi. 
        Tối ưu: Chỉ xét các ô trống trong bán kính 2 ô quanh các quân đã đánh.
        Tuy nhiên, luôn ưu tiên các nước đi thắng ngay lập tức hoặc chặn đối thủ thắng."""
        if not self.history:
            return [(self.size // 2, self.size // 2)]
            
        look_range = 2
        occupied = set((r, c) for r, c, p in self.history)
        candidates = set()
        
        for r, c in occupied:
            for dr in range(-look_range, look_range + 1):
                for dc in range(-look_range, look_range + 1):
                    nr, nc = r + dr, c + dc
                    if 0 <= nr < self.size and 0 <= nc < self.size and self.grid[nr][nc] == 0:
                        candidates.add((nr, nc))

        # Kiểm tra nước đi thắng/chặn ngay trong danh sách candidates để tăng tốc
        win_moves = []
        block_moves = []
        opponent = 3 - self.current_player
        for r, c in candidates:
            # Thử thắng
            self.grid[r][c] = self.current_player
            if self._check_at(r, c) == self.current_player:
                win_moves.append((r, c))
            self.grid[r][c] = 0
            
            # Thử chặn
            self.grid[r][c] = opponent
            if self._check_at(r, c) == opponent:
                block_moves.append((r, c))
            self.grid[r][c] = 0

        if win_moves:
            return list(set(win_moves))
        if block_moves:
            return list(set(block_moves))
        
        # Fallback: nếu không có ứng viên nào trong bán kính nhưng bàn cờ còn trống nhiều,
        # thì có thể cần mở rộng tìm kiếm bằng cách trả về tất cả các ô trống.
        # Điều này tránh trường hợp AI bị "kẹt" khi không có quân nào gần đó.
        if not candidates and len(occupied) < self.size * self.size / 2:
            all_empty_moves = []
            for r in range(self.size):
                for c in range(self.size):
                    if self.grid[r][c] == 0:
                        all_empty_moves.append((r,c))
            return all_empty_moves

        return list(candidates)

# Ví dụ sử dụng:
if __name__ == "__main__":
    board = Board(15)
    board.make_move(7, 7) # Đánh vào giữa
    board.make_move(8, 8) # Đánh vào ô kế tiếp
    board.display()
    board.undo_move()
    board.display()
</file>

<file path="source/utils.py">
import csv
import os

class GameLogger:
    def __init__(self, filename="logs/experiment_results.csv"):
        self.filename = filename
        # Tạo thư mục logs nếu chưa có
        os.makedirs(os.path.dirname(self.filename), exist_ok=True)
        
        # Khởi tạo file với header nếu file chưa tồn tại
        if not os.path.exists(self.filename):
            with open(self.filename, mode='w', newline='', encoding='utf-8') as f:
                writer = csv.writer(f)
                writer.writerow(['Algorithm', 'Depth', 'Nodes_Visited', 'Time_Seconds', 'Score', 'Move'])

    def log_result(self, algo, depth, nodes, duration, score, move):
        """Ghi kết quả thực nghiệm của một nước đi vào file CSV."""
        with open(self.filename, mode='a', newline='', encoding='utf-8') as f:
            writer = csv.writer(f)
            writer.writerow([algo, depth, nodes, f"{duration:.6f}", score, str(move)])

class TrainingLogger:
    def __init__(self, filename="logs/training_history.csv"):
        self.filename = filename
        # Tạo thư mục logs nếu chưa có
        os.makedirs(os.path.dirname(self.filename), exist_ok=True)
        
        # Header ghi lại lịch sử tiến hóa
        if not os.path.exists(self.filename):
            with open(self.filename, mode='w', newline='', encoding='utf-8') as f:
                writer = csv.writer(f)
                writer.writerow(['Generation', 'Challenger_Weights', 'Challenger_Def', 'Is_Better', 'Best_Weights', 'Best_Def'])

    def log_generation(self, gen, c_weights, c_def, is_better, b_weights, b_def):
        """Ghi lại thông tin của một thế hệ huấn luyện."""
        with open(self.filename, mode='a', newline='', encoding='utf-8') as f:
            writer = csv.writer(f)
            writer.writerow([gen, str(c_weights), c_def, is_better, str(b_weights), b_def])
</file>

<file path="play.py">
from source.gomoku import Board
from source.AI import CaroAI
from source.utils import GameLogger

def run_combined_game():
    try:
        from gui.interface import CaroGUI
    except ImportError:
        print("\n[!] Lỗi: Bạn chưa cài đặt thư viện 'pygame' để chạy chế độ GUI.")
        print("[!] Hãy chạy lệnh: pip install pygame")
        return

    # Khởi tạo các thành phần
    size = 9
    board = Board(size)
    ai_depth = 3
    ai_mode = "alpha_beta"
    ai = CaroAI(player_id=2, depth=ai_depth)
    logger = GameLogger()

    print(f"\n--- CARO AI: GUI MODE WITH TERMINAL LOGGING ---")
    print(f"AI Depth: {ai_depth} | Algorithm: {ai_mode}")
    board.display()

    # Kỹ thuật Monkey Patching: Ghi đè phương thức để in ra terminal khi chơi trên GUI
    original_make_move = board.make_move
    def patched_make_move(r, c):
        current_p = board.current_player
        success = original_make_move(r, c)
        if success and not board.is_searching: # Chỉ in log nếu không phải AI đang tìm kiếm
            symbol = 'X' if current_p == 1 else 'O'
            print(f"\n[Move] Người chơi {current_p} ({symbol}) đánh vào: ({r}, {c})")
            board.display()
            winner = board.check_win()
            if winner != 0:
                if winner == -1: print(">>> KẾT THÚC: HÒA <<<")
                else: print(f">>> KẾT THÚC: NGƯỜI CHƠI {winner} THẮNG <<<")
        return success
    
    original_get_move = ai.get_move
    def patched_get_move(board_state, mode=ai_mode):
        # Gọi hàm gốc để tính toán
        move, score, nodes, duration = original_get_move(board_state, mode)
        if not board_state.is_searching: # Chỉ in log kết quả cuối cùng
            print(f"\n[AI Thinking] {nodes} nodes duyệt, thời gian: {duration:.4f}s")
            print(f"[AI Choice] Đánh vào {move} với điểm đánh giá: {score}")
        logger.log_result(mode, ai_depth, nodes, duration, score, move)
        return move, score, nodes, duration

    # Gán các hàm đã được "nâng cấp" vào đối tượng
    board.make_move = patched_make_move
    ai.get_move = patched_get_move

    gui = CaroGUI(board, ai)
    gui.run()

if __name__ == "__main__":
    run_combined_game()
</file>

<file path="source/AI.py">
import time

class CaroAI:
    def __init__(self, player_id, depth=3, weights=None, defense_multiplier=2.0):
        self.player_id = player_id
        self.opponent_id = 3 - player_id
        self.depth = depth
        self.nodes_visited = 0
        # Nếu không truyền weights, sử dụng bộ trọng số mặc định
        self.weights = weights if weights else {3: 10000, 2: 500}
        self.defense_multiplier = defense_multiplier
        self.transposition_table = {}  # {hash: (depth, flag, value, best_move)}
        self.killer_moves = [[None] * 2 for _ in range(20)]  # Lưu 2 nước killer mỗi độ sâu
        self.start_time = 0
        self.time_limit = 2.0

    def evaluate_board(self, board):
        """
        Hàm đánh giá Heuristic nâng cao.
        Gán điểm số dựa trên các mẫu hình (patterns) của cửa sổ 4 ô.
        """
        score = 0
        # Trọng số điểm cho các tình huống:
        # 4 quân: Thắng tuyệt đối.
        # 3 quân + 1 ô trống: Cực kỳ nguy hiểm.
        # 2 quân + 2 ô trống: Tiềm năng tạo thế công.
        score += self.calculate_player_score(board, self.player_id, self.weights)
        # Hệ số phòng thủ cao để AI cực kỳ cẩn thận với chuỗi của đối thủ
        score -= self.calculate_player_score(board, self.opponent_id, self.weights) * self.defense_multiplier
        return score

    def calculate_player_score(self, board, player, weights):
        total_score = 0
        directions = [(0, 1), (1, 0), (1, 1), (1, -1)]
        
        for r in range(board.size):
            for c in range(board.size):
                for dr, dc in directions:
                    p_count, e_count, opp_count = 0, 0, 0
                    for i in range(4):
                        nr, nc = r + dr * i, c + dc * i
                        if 0 <= nr < board.size and 0 <= nc < board.size:
                            v = board.grid[nr][nc]
                            if v == player: p_count += 1
                            elif v == 0: e_count += 1
                            else: 
                                opp_count = 1
                                break
                        else:
                            opp_count = 1
                            break

                    # Nếu cửa sổ không bị đối thủ chặn và có quân của mình
                    if opp_count == 0 and p_count > 0:
                        # Ưu tiên lấy điểm từ bộ trọng số truyền vào
                        total_score += weights.get(p_count, p_count * 10)
        return total_score

    def score_move_quick(self, board, r, c):
        """Đánh giá nhanh độ 'hot' của một ô để sắp xếp nước đi."""
        score = 0
        for dr in range(-1, 2):
            for dc in range(-1, 2):
                if dr == 0 and dc == 0: continue
                nr, nc = r + dr, c + dc
                if 0 <= nr < board.size and 0 <= nc < board.size and board.grid[nr][nc] != 0:
                    score += 1
        return score

    def minimax(self, board, depth, is_maximizing):
        self.nodes_visited += 1
        result = board.check_win()
        if result == self.player_id: return 1000000, None
        if result == self.opponent_id: return -1000000, None
        if result == -1: return 0, None
        if depth == 0: return self.evaluate_board(board), None

        moves = board.get_legal_moves()
        # Sắp xếp nước đi: Ưu tiên ô có nhiều quân xung quanh + gần trung tâm
        moves.sort(key=lambda m: (
            self.score_move_quick(board, m[0], m[1]), 
            -abs(m[0] - board.size // 2) - abs(m[1] - board.size // 2)
        ), reverse=True)
        
        best_move = moves[0] if moves else None

        if is_maximizing:
            best_score = -float('inf')
            for r, c in moves:
                board.make_move(r, c)
                score, _ = self.minimax(board, depth - 1, False)
                board.undo_move()
                if score > best_score:
                    best_score = score
                    best_move = (r, c)
            return best_score, best_move
        else:
            best_score = float('inf')
            for r, c in moves:
                board.make_move(r, c)
                score, _ = self.minimax(board, depth - 1, True)
                board.undo_move()
                if score < best_score:
                    best_score = score
                    best_move = (r, c)
            return best_score, best_move

    def alpha_beta(self, board, depth, alpha, beta, is_maximizing):
        self.nodes_visited += 1

        # Kiểm tra thời gian định kỳ để dừng tìm kiếm
        if self.nodes_visited % 1000 == 0:
            if time.time() - self.start_time > self.time_limit:
                return None, None

        board_hash = board.get_hash()
        alpha_orig = alpha
        beta_orig = beta
        tt_move = None

        # 1. Tra cứu bảng Transposition (Bộ nhớ đệm trạng thái)
        if board_hash in self.transposition_table:
            tt_depth, tt_flag, tt_val, tt_move = self.transposition_table[board_hash]
            if tt_depth >= depth:
                if tt_flag == "EXACT": return tt_val, tt_move
                if tt_flag == "LOWER": alpha = max(alpha, tt_val)
                elif tt_flag == "UPPER": beta = min(beta, tt_val)
                if alpha >= beta: return tt_val, tt_move

        result = board.check_win()
        # Thêm +depth để AI ưu tiên thắng nhanh nhất và thua chậm nhất
        if result == self.player_id: return 1000000 + depth, None
        if result == self.opponent_id: return -1000000 - depth, None
        if result == -1: return 0, None
        if depth == 0: return self.evaluate_board(board), None

        moves = board.get_legal_moves()
        # 2. Cải tiến Move Ordering với Killer Heuristic
        killers = self.killer_moves[depth]
        moves.sort(key=lambda m: (
            m == tt_move, # Ưu tiên nước đi tốt nhất từ bảng băm (ngay cả khi depth thấp)
            m in killers, # Ưu tiên các nước đi "sát thủ" đã tìm thấy trước đó
            self.score_move_quick(board, m[0], m[1]), 
            -abs(m[0] - board.size // 2) - abs(m[1] - board.size // 2)
        ), reverse=True)
        
        best_move = moves[0] if moves else None

        if is_maximizing:
            best_score = -float('inf')
            for r, c in moves:
                board.make_move(r, c)
                score, _ = self.alpha_beta(board, depth - 1, alpha, beta, False)
                board.undo_move()

                if score is None: 
                    return None, None # Thoát do hết thời gian

                if score > best_score:
                    best_score = score
                    best_move = (r, c)
                alpha = max(alpha, best_score)
                if beta <= alpha:
                    # Lưu Killer move
                    if killers[0] != (r, c):
                        killers[1] = killers[0]
                        killers[0] = (r, c)
                    break
        else:
            best_score = float('inf')
            for r, c in moves:
                board.make_move(r, c)
                score, _ = self.alpha_beta(board, depth - 1, alpha, beta, True)
                board.undo_move()

                if score is None: 
                    return None, None # Thoát do hết thời gian

                if score < best_score:
                    best_score = score
                    best_move = (r, c)
                beta = min(beta, best_score)
                if beta <= alpha:
                    # Lưu Killer move
                    if killers[0] != (r, c):
                        killers[1] = killers[0]
                        killers[0] = (r, c)
                    break

        # 3. Lưu kết quả vào Transposition Table
        if best_score <= alpha_orig:
            flag = "UPPER"
        elif best_score >= beta_orig:
            flag = "LOWER"
        else:
            flag = "EXACT"
        self.transposition_table[board_hash] = (depth, flag, best_score, best_move)
        
        return best_score, best_move

    def get_move(self, board, mode="alpha_beta", time_limit=2.0):
        self.nodes_visited = 0
        self.start_time = time.time()
        self.time_limit = time_limit

        if mode == "minimax":
            score, move = self.minimax(board, self.depth, True)
            return move, score, self.nodes_visited, time.time() - self.start_time

        final_best_move = None
        final_best_score = 0

        # Iterative Deepening: Tìm kiếm sâu dần để tận dụng thời gian và Move Ordering
        for current_depth in range(1, self.depth + 1):
            score, move = self.alpha_beta(board, current_depth, -float('inf'), float('inf'), True)
            
            if score is None: # Hết thời gian, dừng ở độ sâu trước đó
                break
                
            final_best_move = move
            final_best_score = score
            
            # Nếu đã tìm thấy nước thắng tuyệt đối, dừng ngay
            if abs(final_best_score) >= 900000:
                break

        return final_best_move, final_best_score, self.nodes_visited, time.time() - self.start_time
</file>

</files>
